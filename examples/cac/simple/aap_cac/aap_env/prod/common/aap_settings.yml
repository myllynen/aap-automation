#
# AAP per-environment settings - prod
#

# AAP/platform definitions
aap_hostname: aap.example.com

aap_validate_certs: true

# AAP/environment definitions
aap_ad_server_uri:
  - ldap://ad.example.com:389
#aap_ad_bind_username: cn=aap-ldap,ou=services,ou=corp,dc=example,dc=com
#aap_log_aggregator_username: aap-logger
