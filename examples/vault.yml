# Ansible Automation Platform vault - unencrypted example

#
# AAP/platform credentials
#

# AAP administrator credentials
aap_admin_username: admin
aap_admin_password: Foobar_12

# AAP username and password for AAP CaC playbooks and roles
aap_username: "{{ aap_admin_username }}"
aap_password: "{{ aap_admin_password }}"

# AAP gateway token
# This needs to be added after installation
#aap_token: "{{ lookup('file', lookup('env', 'PWD') + '/aap_token.txt') }}"

# AAP automation hub token
# This needs to be added after installation
#hub_token: "{{ lookup('file', lookup('env', 'PWD') + '/pah_token.txt') }}"

#
# AAP/Red Hat credentials
#

# Red Hat Automation Hub token
# Needed for configuring automation hub
# https://console.redhat.com/ansible/automation-hub/token
#aap_redhat_hub_token: "{{ lookup('file', lookup('env', 'PWD') + '/rh_token.txt') }}"

# Red Hat registry credentials
# Needed for configuring automation hub
#aap_redhat_registry_username: your-rhn-username
#aap_redhat_registry_password: your-rhn-password

# Red Hat service account credentials
# Needed for configuring automation analytics
#aap_redhat_client_id: your-rhn-client-id
#aap_redhat_client_secret: your-rhn-client-secret

#
# AAP/environment credentials
#

#aap_ad_bind_password: changeme
#aap_log_aggregator_password: changeme
