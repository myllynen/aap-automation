aap_hostname: gateway.example.com

aap_username: "{{ aap_admin_username | default('admin') }}"
aap_password: "{{ aap_admin_password }}"
#aap_token: "{{ lookup('file', lookup('env', 'PWD') + '/aap_token_admin.txt') }}"

aap_validate_certs: false
